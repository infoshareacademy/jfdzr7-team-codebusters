import styled from "styled-components";
import { CategoryBar } from "./CategoryBar";

export const StyledCategoryBar = styled(CategoryBar)`
    padding: 20px;
    display: flex;
    justify-content: space-between;
`