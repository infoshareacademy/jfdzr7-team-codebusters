import styled from "styled-components";
import { PosiotionHeader } from "./PositionHeader";

export const StyledPositionHeader = styled(PosiotionHeader)`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
`